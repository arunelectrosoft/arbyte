# Brand Icon

Modern embedded systems academy logo, redesigned as a compact circuit-to-AI
motif: a chip (MCU) feeding an AI core over a circuit trace, styled in the
brand's burgundy (#8d021f) so it reads as a mark/logo rather than a generic
flowchart.

```mermaid
flowchart TD
  classDef chip fill:#8d021f,stroke:#5a0114,stroke-width:2px,color:#ffffff,font-weight:bold;
  classDef ai fill:#ffffff,stroke:#8d021f,stroke-width:2px,color:#8d021f,font-weight:bold;

  MCU[["MCU"]]:::chip --> AI(("AI")):::ai
```
